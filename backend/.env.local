# === BridgeLineUSA Backend (local secrets; NOT committed) ===
# Turn Supabase storage ON
USE_SUPABASE=1

# Your project URL (from Supabase > Project Settings > API)
SUPABASE_URL=https://fypkufdjzjmjpuabasqd.supabase.co

# Service Role key (from Supabase > Project Settings > API)
# (Do NOT use the anon key here; we need service role for server-side uploads + signed URLs)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImZ5cGt1ZmRqemptanB1YWJhc3FkIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc1NjYwMzYyNSwiZXhwIjoyMDcyMTc5NjI1fQ.q8S0zeFEdhV713ANtlSRitbajpOr3Bx7ZdxKDkfykDI

# Optional: explicit bucket name (or leave blank to use fallback 'blusa-uploads-prod')
SUPABASE_BUCKET_UPLOADS=blusa-uploads-prod

# Backend server
PORT=4000

# Filesystem roots
QUOTE_ROOT=/workspaces/BridgeLineUSA/data/quotes
UPLOADS_DIR=/workspaces/BridgeLineUSA/backend/data/uploads

# Database (adjust if you add one later)
DATABASE_URL=file:/workspaces/BridgeLineUSA/backend/data/app.db
JWT_SECRET=change_this_secret
