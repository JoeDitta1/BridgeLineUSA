SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
USE_SUPABASE=0
PORT=4000
DATA_ROOT=./data
UPLOADS_DIR=./uploads

# When you create a local `backend/.env.local`, DO NOT commit it to git.
# Add actual secrets to backend/.env.local (this file is purposely empty of secrets).# Backend server
PORT=4000

# Filesystem roots
QUOTE_ROOT=C:\SCM-AI\data\quotes
UPLOADS_DIR=C:\SCM-AI\backend\uploads

# Database (adjust if you add one later)
DATABASE_URL=file:C:\SCM-AI\data\app.db
JWT_SECRET=change_this_secret
