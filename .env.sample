# Example values — copy to your local .env files and replace.
# Backend service URL used by the frontend:
REACT_APP_API_BASE=http://localhost:4000

# Suggest a QUOTE root (used by backend to create folders per quote)
QUOTE_ROOT=C:\SCM-AI\data\quotes
